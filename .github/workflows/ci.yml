name: CI Pipeline

on:
  push:
    branches: [ github-actions, docker-lab , docker-compose-lab]
  pull_request:
    branches: [ github-actions, docker-lab, docker-compose-lab]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: pip install pytest
      - name: Run unit tests
        run: python -m pytest tests/my_unit_test.py -v
        

      
      - name: login 
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.CR_PAT }}
          
      - name:  push image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/derasoft004/catty-app-test:latest
            ghcr.io/derasoft004/catty-app-test:${{ github.sha }}
            
